import { TemplateCaptureDirective } from './template-capture.directive';

describe('TemplateCaptureDirective', () => {
  it('should create an instance', () => {
    const directive = new TemplateCaptureDirective();
    expect(directive).toBeTruthy();
  });
});
