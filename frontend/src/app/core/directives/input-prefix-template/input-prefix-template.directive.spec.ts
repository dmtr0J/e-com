import { InputPrefixTemplateDirective } from './input-prefix-template.directive';

describe('InputPrefixTemplateDirective', () => {
  it('should create an instance', () => {
    const directive = new InputPrefixTemplateDirective();
    expect(directive).toBeTruthy();
  });
});
