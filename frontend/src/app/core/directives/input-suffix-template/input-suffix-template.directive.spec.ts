import { InputSuffixTemplateDirective } from './input-suffix-template.directive';

describe('InputSuffixTemplateDirective', () => {
  it('should create an instance', () => {
    const directive = new InputSuffixTemplateDirective();
    expect(directive).toBeTruthy();
  });
});
